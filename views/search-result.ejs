<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Search Results</title>
</head>
<body>

<h1>Search Results for "<%= keyword %>"</h1>

<p><a href="/search">New search</a></p>

<% if (results.length === 0) { %>
<p>No books found.</p>
<% } else { %>

<table border="1" cellpadding="5">
<thead>
<tr>
    <th>Title</th><th>Author</th><th>Price</th>
</tr>
</thead>
<tbody>
<% results.forEach(b => { %>
<tr>
    <td><%= b.title %></td>
    <td><%= b.author %></td>
    <td>£<%= b.price %></td>
</tr>
<% }) %>
</tbody>
</table>

<% } %>

<p><a href="/list">Back to all books</a></p>

</body>
</html>
