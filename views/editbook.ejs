<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Edit Book – <%= shopData.shopName %></title>
</head>
<body>
  <h1>Edit Book</h1>

  <form method="POST" action="/books/edit/<%= book.id %>">
    <p>
      Book name:
      <input type="text" name="name" value="<%= book.name %>" required>
    </p>
    <p>
      Price (£):
      <input type="number" name="price" step="0.01" min="0"
             value="<%= book.price %>" required>
    </p>
    <p>
      <button type="submit">Save changes</button>
    </p>
  </form>

  <p><a href="/books/list">Back to list</a></p>
</body>
</html>
