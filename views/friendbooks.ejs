<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>My friend’s books</title>
</head>
<body>
  <h1>My friend’s books</h1>

  <ul>
    <% books.forEach(function(book) { %>
      <li>
        <strong><%= book.name %></strong>
        – £<%= book.price %>
      </li>
    <% }) %>
  </ul>

  <p><a href="/">Back to home</a></p>
</body>
</html>
